# https://docs.zephyrproject.org/latest/guides/modules.html
name: sof
samples:
    - app/

build:
  settings:
    dts_root: .
# Default values:
# build:
#  cmake: ./zephyr
#  kconfig: ./zephyr/Kconfig
